import { createTheme } from '@mui/material';
const theme = createTheme({
  palette: {
    primary: {
      main: '#578e44',
    },
    secondary: {
      main: '#dc143c',
    },
  },
});

export default theme;
